#!/bin/bash
# Pre-push hook: run medium local CI tier before every push.
#
# To install hooks: scripts/install-hooks.sh

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
"$SCRIPT_DIR/ci-local" --tier medium
