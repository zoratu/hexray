#!/bin/bash
# Pre-commit hook: run fast local CI tier before every commit.
#
# To install hooks: scripts/install-hooks.sh

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
"$SCRIPT_DIR/ci-local" --tier fast
