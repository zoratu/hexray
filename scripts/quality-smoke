#!/bin/bash
# Fast decompiler quality smoke check.
# Runs only fixture-backed control-flow quality benchmark cases.

set -euo pipefail

ROOT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT_DIR"

cargo test -p hexray-analysis --test control_flow_quality_smoke -- --nocapture
