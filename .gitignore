# Rust
/target/
fuzz/target/
**/*.rs.bk
Cargo.lock

# IDE / agent local state
.idea/
.vscode/
.claude/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Test artifacts
*.exe
*.dll
*.so
*.dylib

# ...but the committed PE test fixtures are checked-in source artifacts
# (tiny, deterministic, `include_bytes!`'d by hexray-formats / cli_tests).
# Without these a clean checkout / `git archive` can't compile the tests.
# Regenerate via tests/fixtures/pe/create_pe*.py if ever needed.
!tests/fixtures/pe/simple_x64.exe
!tests/fixtures/pe/complex_x64.exe

# Build artifacts
*.pdb
*.proptest-regressions

# Test binaries
ls
nfsd.ko
tests/fixtures/test_globals
tests/fixtures/test_goto3
tests/fixtures/test_gotos
tests/fixtures/test_gotos2
tests/fixtures/test_simple_goto
tests/fixtures/test_structs
